Single-select pill row (e.g. instrument-type filters); active chip goes solid ink.

```jsx
<FilterChips value={f} onChange={setF} options={[{value:'all',label:'Бүгд'},{value:'cd',label:'Сертификат',color:'#2D6BFF'}]} />
```

Optional per-option color dot aids scanning.
