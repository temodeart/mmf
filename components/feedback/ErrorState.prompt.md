Inline alert with glyph + tone; always pairs a cause with a retry / next action.

```jsx
<ErrorState variant="neg" title="Алдаа гарлаа" action={{label:'Дахин ачаалах', onClick:retry}} />
```

neg (error) · warn (caution) · info (neutral).
