Primary-market product card with the rate as the hero and an availability meter.

```jsx
<InstrumentCard data={inst} selected={sel} onSelect={pick} onBuy={buy} />
```

type sets the letter-mark gradient. Selected adds indigo outline + ring.
