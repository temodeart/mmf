52px labeled field with focus ring and inline error; use mono for numeric values.

```jsx
<TextInput label="Тоо ширхэг" mono value={qty} onChange={onQty} />
```

Focus lifts fill to white + indigo ring. error swaps border/message to --neg.
