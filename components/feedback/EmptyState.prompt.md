Mark + one human line + a primary action; each surface passes its own Mongolian copy.

```jsx
<EmptyState title="Таны багц хоосон байна" action={{label:'Бүтээгдэхүүн үзэх', onClick:go}} />
```

Never leave a data surface blank — use this for empty tables, charts and lists.
