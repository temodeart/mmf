Shimmer placeholder matching the final layout — never a bare spinner.

```jsx
<Skeleton variant="num" width="72%" />
```

Requires the .skeleton keyframes from the design system base CSS (styles.css).
