Sticky content header: page title + notification bell + user chip (name, never raw email).

```jsx
<Topbar title="Арилжаа" userName="Тэмүүжин Батбаяр" initials="ТБ" notifCount={2} />
```

Bell shows an orange dot when notifCount > 0. Frosted-glass surface matching Sidebar.
